import React, { useState } from 'react';

const NewsList = () => {
    const [search, setSearch] = useState<string>('');
    return (
        <div>
            news List
        </div>
    );
};

export default NewsList;